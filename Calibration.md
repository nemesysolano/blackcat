cmake -B cmake_build_debug -DLITERT_ENABLE_SAMSUNG=OFF

cmake -S . -B build-release \
    -DLITERT_ENABLE_SAMSUNG=OFF
    -DCMAKE_BUILD_TYPE=Release \
    -DLITERT_AUTO_BUILD_TFLITE=ON \
    -DLITERT_ENABLE_GPU=ON \
    -DLITERT_ENABLE_NPU=ON \
    -DLITERT_DISABLE_KLEIDIAI=ON \
    -DLITERT_HOST_C_COMPILER=/usr/bin/clang \
    -DLITERT_HOST_CXX_COMPILER=/usr/bin/clang++
cmake --build build-release --target dispatch_api_Qualcomm_so -j8

Look, `test_litert_availability_test` in `main.cpp` ran successfully and the `Makefile` indicates the LiteRT dependencies we linked. Now I need to modify `test_litert_availability_test` to make test a keras model generated by `create_fractional_diff_model`. Tell me how to convert a keras model into LiteRT format and test it inside `test_litert_availability_test`.