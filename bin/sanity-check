#!/usr/bin/env bash
SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"

for QUOTE in $(cat ${SCRIPT_DIR}/../documents/quotes-mini.txt)
do
    for INDICATOR in $(cat ${SCRIPT_DIR}/../documents/indicators.txt)
    do
        train-model $QUOTE $INDICATOR
    done
done
